# Design

<!-- impeccable:design-schema 1 -->

## Direction

以“青绿山水与药材标本册”为视觉世界：登录页基于用户提供的参考图重新裁切、调色与模糊处理，形成可收放的全幅画面；业务界面像一本安静的诊疗工作册，使用米白纸张、松柏绿和少量朱砂。公开分发图片素材前需单独确认原图授权。

## Palette

- `--ink`: `#17352e`，正文与深色标题。
- `--pine`: `#245c4a`，主操作、导航和可靠状态。
- `--jade`: `#4e8c75`，次级操作、图表和聚焦边框。
- `--herb`: `#8fa88f`，弱化信息与分隔。
- `--paper`: `#f5f1e8`，全局纸张底色。
- `--paper-raised`: `#fffdf8`，卡片与表单表面。
- `--cinnabar`: `#b64832`，警示、异常和高风险操作。
- `--gold`: `#b18a4a`，待处理状态与轻微强调。
- `--line`: `#d9d4c7`，边框。

## Typography

- 中文正文：`"Source Han Sans SC", "Noto Sans CJK SC", "Microsoft YaHei"`，正文 14-16px，行高 1.65。
- 展示标题：`"Source Han Serif SC", "Noto Serif CJK SC", "SimSun"`，仅在登录页品牌标题和页面主标题使用。
- 数字和金额使用 `font-variant-numeric: tabular-nums`。

## Layout

- 登录页桌面端双栏：左侧 60% 参考图二次处理后的青绿视觉，右侧 40% 米白登录册页；移动端只保留精简背景与单列表单。
- 业务页沿用现有信息架构，但统一为浅纸背景、深绿顶栏、白色内容面板、8/12/16/24 间距节奏。
- 门店控制台只对连锁总店显示，使用聚合数字与门店表，不展示患者明细。

## Components

- 按钮：主按钮松柏绿实底，次按钮描边，危险按钮朱砂；圆角 8px，不叠加大面积阴影。
- 卡片：1px 暖灰边框，圆角 12px，避免层层嵌套。
- 表单：清晰标签，聚焦时青玉环；错误文字靠近字段。
- 状态：待收费用金色、已收费用松柏绿、异常用朱砂；同时显示文字，不只依赖颜色。
- 打印：收费单 A4 与 80mm 热敏纸分别使用专用 `@media print` 规则。

## Motion

只保留一个品牌化入场：登录页山岚与右侧册页在 600ms 内错层进入；其余交互使用 160-220ms 的透明度、位移或边框变化，并尊重 `prefers-reduced-motion`。

## Accessibility

保持键盘焦点可见；控件和文字对比度达到 WCAG AA；触控目标至少 44px；表单错误使用可读文本和 `aria-live`。